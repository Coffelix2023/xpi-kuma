## 1. 架构边界与数据模型

- [x] 1.1 按 README 的进程边界要求写架构决策记录，说明 Pi 实时采集、共享补录和独立网页/探测的所有权；验证记录与四份 delta spec 一致。（docs/adr/0001-process-boundaries.md）
- [x] 1.2 为 usage_records 增加可空的来源日志条目 ID、单独的消息时间与实时/日志对账指纹及唯一来源索引，只做向前迁移；通过打开旧数据库、重复迁移和保留原行的测试。
- [x] 1.3 实时 `message_end` 除现有完成时间外另存 `message.timestamp`，用已有会话、模型、token 与费用字段计算对账指纹，内容摘要只做可选消歧；验证两次相同 token 但不同消息分别入账，且新行的消息时间与完成时间正确区分。

## 2. 持续采集与历史补录

- [x] 2.1 将 Pi 内 `off` 改为仅停网页和探测，不关闭实时采集器与数据库；通过 `off` 后发送 `message_end` 并检查数据库新增一行的测试。
- [x] 2.2 实现共享、按配置保留期限制的 Pi 会话 JSONL 扫描器，解析 header、条目身份与 assistant usage，逐文件隔离损坏/不可读错误；验证跨项目日志、坏文件和超期日志样例。
- [x] 2.3 实现基于来源条目唯一约束与指纹的一次性补录/实时对账事务，冲突或旧行不可判定时跳过并报告未核实数量和日志位置；验证重复扫描、实时与日志重叠、旧库无指纹和指纹碰撞均不重复计数。
- [x] 2.4 在 Pi `session_start` 的保留期清理之后运行共享补录，扫描下界与清理截止时间一致，且不阻断后续正常采集；验证存在历史缺口时统计补齐、超期行不被反复插入又清除、补录延迟字段保持未知。
- [x] 2.5 在独立服务 `on` 完成首次页面就绪前调用同一补录逻辑，部分失败仍启动且展示未核实诊断；验证 Pi 未运行时重新启动服务后首屏含已补记录，以及两入口并发补录只入账一次。

## 3. 服务所有权与终端入口

- [x] 3.1 增加共享单实例状态与排他所有权协调，记录进程身份和实际端口，校验陈旧状态与 PID 复用后才允许 `off` 发信号；验证并发启动只产生一个所有者、复用 PID 不误杀其他进程。
- [x] 3.2 调整 Pi 内服务启动和探测任务管理：独立服务已运行则 `/xpi-kuma` 复用其 URL，Pi 不重复监听/探测，Pi 已先启动则独立 `on` 明确拒绝；通过双进程单监听和单组定时探测测试。
- [x] 3.3 新增全局 `xpi-kuma` 可执行入口及安装方式，支持 `on --port`、`off`、`status`、参数校验和真实端口反馈；验证非法端口不写状态、占用端口明确失败、重复 on/off 幂等。
- [x] 3.4 用 Node detached spawn 启动独立服务，stdin 忽略，stdout/stderr 重定向日志，`unref()` 并等待真实就绪；验证调用终端退出后仍提供网页、`off` 有界关闭、宿主 stderr 零字节（含未隔离对照组）。
- [x] 3.5 让独立服务复用现有 dashboard 凭据、账户/OAuth、配置热重载、数据库与探测模块，并遵循 `PI_CODING_AGENT_DIR`；验证现有 API 权限、配置编辑/回调、共享数据目录和回环地址不变。

## 4. 文档与整体验收

- [x] 4.1 更新 README.md、README.zh-CN.md 的安装、命令、服务归属及用量补录/未核实提示，保持仓库架构记录同步；验证文档命令与 CLI 实际行为一致。
- [x] 4.2 完成组合验收：Pi 服务开关期间用量不断录，Pi 关闭后后台网页/探测继续，重新 `on` 立即补齐保留期内可识别日志，冲突明确报告且不双计；验证集成测试与手动终端演练。
- [x] 4.3 分别运行 `pnpm typecheck`、`pnpm -w run lint`、`pnpm test` 并确保全部通过，随后按仓库提交纪律建立可回滚检查点；以命令退出状态与 git diff 核实。
