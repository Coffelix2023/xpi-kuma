# DELTA — hifi vs wireframe (kuma-dashboard / accounts)

> 上游线框：`pages/accounts/wireframe/` v1。结构继承线框，视觉自由。

| 日期 | 线框决策 | hifi 改动 | 原因 | 批准 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-09-16 | 来源标记用 `border-style`（实线/虚线/点线）在灰阶里区分 | 保留同一套边线编码，升级为圆角徽标 + 更强对比的文字 | 线框受四 token 限制；编码本身要保住，换的是呈现 | 待用户确认 |
| 2026-09-16 | 未知值用 `.wf-dim` 弱化 | 改用 `--muted-foreground` 并把动作按钮加粗为次要强调 | 让「未知」与正常数字的差别一眼可见，同时把下一步动作顶出来 | 待用户确认 |
| 2026-09-16 | 全局等宽字体 | UI 用 `--font-sans`，金额用 `--font-mono` + `tabular-nums` | 与主面板同一决定，两页观感必须一致 | 待用户确认 |
| 2026-09-16 | 数据来源三档是行内小标签 | 表头下方补一行图例（实线/虚线/点线） | 三档编码不看说明猜不出来 | 待用户确认 |
| 2026-09-16 | 空态在本页是独立文件 `02-empty.html`（主面板 `P2`） | 本页空态仍放在同一文件内作为 `P3-4` 块 | 线框如此，且该块同时承担「取数顺序说明」职责 | 待用户确认 |

| 2026-09-16 | 页头只有「返回主面板」与「同步全部余额」 | 未新增主题/语言按钮，改为读取主面板写入的偏好 | 线框页头没有这两个控件；新增会引入字典外短码 | 待用户确认 |
| 2026-09-16 | 账户页无主题入口 | 读取主面板写入的 `kuma.family`，跟随家族切换 | 与既有「本页不新增主题按钮」的决定一致 | 待用户确认 |

| 2026-09-16 | 账户页沿用 Default 视觉 | 随家族切换套用方格纸底、标题带、卡片强调线（但无索引轨，页头无按钮） | 与主面板保持同一套印刷家族语言 | 用户已指定 |

未偏离项：六列顺序、四行状态覆盖、短码集合、空态三条说明、「未知」而非 ¥0.00 的规则。
