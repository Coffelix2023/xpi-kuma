# tasks.md — hifi (kuma-dashboard / settings)

> 本阶段的**任务清单与进度**，与 `plan.md` 一起在每轮深挖后覆写，不新建副本。
> 编号顺序即产出顺序；只有非线性依赖才在行尾补 `(依赖: 1.2)`。
> 状态机：待执行是空方框；进行中是空方框加行尾 `⏳ in_progress`；完成是打勾方框并紧跟一条验证子行。
> 一次只推进一项：先标进行中，产出后立刻勾选并写验证子行，不批量补勾。

## 任务

- [x] 8.1 生成配置体检页骨架 (验收:HTML 结构完整、五块占位与 id=P4-1..P4-5 就位、空 style/script 存在;产出:current/screens/01-settings.html)
  验证: 骨架 5 块 id=P4-1..P4-5 就位；style 逐字复用主面板（542 行，含两家族与标题带），另加体检页增量样式 · 10:05
- [x] 8.2 写入 token 与基座样式 (验收:与既有三页同一套 token、家族覆盖块逐字一致、直角与标题带样式就位;产出:current/screens/01-settings.html)
  验证: 复用样式与主面板 md5 相同来源；max(0px, calc(var(--radius) - 4px)) 保留；家族块存在检查通过 · 10:06
- [x] 8.3 实现头部与配置文件块 (验收:生效路径、来源、解析结果、修改时间四项可见;产出:current/screens/01-settings.html)
  验证: 生效路径 /Users/me/work/app-web/.pi/xpi-kuma/config.yaml、来源（模板生成）、解析通过、修改时间四项可见 · 10:07
- [x] 8.4 实现供应商体检表 (验收:六项检查齐全、每个缺失项配一句下一步动作、密钥只显示环境变量名;产出:current/screens/01-settings.html)
  验证: 六列齐全；四行覆盖 缺 endpoint / 环境变量未设置 / 未配密钥 / 缺 price.output；每处 ✗ 都带一句动作；密钥只显示 $VAR 名 · 10:08
- [x] 8.5 实现全局与存储块 (验收:retention、usage.db 存在性与大小、三项默认值提示可见;产出:current/screens/01-settings.html)
  验证: retention 7 天（默认值）、usage.db 存在 · 1.2 MB、探测默认三值、会话 cwd 四项可见 · 10:09
- [x] 8.6 实现解析失败态与只读边界说明 (验收:解析失败时只显示错误与路径、不显示半截数据;写盘策略写清;产出:current/screens/01-settings.html)
  验证: 解析失败卡只给错误行与文件路径，并写明不渲染 P4-3/P4-4；只读边界三条（只读 / 写盘策略 / 密钥纪律）齐 · 10:10
- [x] 8.7 无障碍与自检 (验收:焦点可见、对比度达标、零外部请求、id 与字典一致;产出:current/screens/01-settings.html)
  验证: 渲染实测：控制台 0 报错；零外部请求；13 个 P4 短码与字典 diff 为空；三页脚本语法通过 · 10:12

## 阻塞与决定

- 无上游线框：本页直接起稿，DELTA 已登记。
- 本轮不实现任何写盘路径；写盘能力留给下一轮，需配测试与备份策略。
