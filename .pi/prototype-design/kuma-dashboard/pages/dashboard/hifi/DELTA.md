# DELTA — hifi vs wireframe (kuma-dashboard / dashboard)

> 上游线框：`pages/dashboard/wireframe/` v2。结构继承线框，视觉自由。
> 每一条偏离线框的**布局或视觉**决策都记在这里；信息层级与 CTA 不变。

| 日期 | 线框决策 | hifi 改动 | 原因 | 批准 |
| :--- | :--- | :--- | :--- | :--- |
| 2026-09-16 | 状态三态用**形状**区分（实心/空心/虚线），线框四 token 限制所致 | 改用颜色：正常 `--foreground`、异常 `--destructive`、未知 `--muted-foreground` | hifi 允许上色；`THEMES.md` 没有 success 绿，不为状态自造 token | 待用户确认 |
| 2026-09-16 | 趋势图用 Chart.js CDN，离线降级隐藏 | 改**内联 SVG** 双轴折线，永不降级 | hifi 硬规则要求单文件自包含、零外部请求 | 待用户确认 |
| 2026-09-16 | 全局等宽字体（线框的终端取向） | UI 用 `--font-sans`，数字与路径用 `--font-mono` | `THEMES.md` 明确定义了 sans / mono 分工；数字列用 `tabular-nums` 依旧对齐 | 待用户确认 |
| 2026-09-16 | 供应商行带虚线块标签（`data-wireframe-block` 角标） | 移除角标，改为普通卡片容器 | 线框角标是评审辅助物，不是产品视觉 | 待用户确认 |

| 2026-09-16 | 主题只有一套 Default-Themes（oklch） | 增加 `--accent-tint` / `--accent-ink` 回退消费与 Atlas 家族覆盖块，页内新增家族切换按钮 | 两套主题家族同名变量值不同，必须用 `data-family` 隔离；`--radius:0rem` 由 `max(0px, calc(...))` 兜住负值 | 待用户确认 |

| 2026-09-16 | Atlas v1（图鉴风，themes-02）亮色结构线是暖灰 `#E9E2D8` | 整个家族换成 Atlas v2（复古印刷，themes-01）：奶油纸 + 普鲁士蓝 + 柿橙 | 用户反馈亮色版缺蓝；themes-01 的结构线就是藏青 `#24425E` | 用户已指定 |
| 2026-09-16 | 无标题带、无索引轨、无方格纸底 | 新增方格纸底纹、藏青分区标题带（橙方块标记）、右侧藏青索引轨（当前项橙底带缺角）、卡片顶部强调线 | 对齐参考截图 `IMG_5132.JPG` 的配色与风格 | 用户已指定 |
| 2026-09-16 | 标题带用 `--primary` | 改用 `--sidebar` 系列 | themes-01 的 `.dark --primary` 是橙色，会让带子在暗色翻色；`--sidebar` 两模式恒定藏青 | 用户已指定 |

| 2026-09-16 | （线框时代只有供应商块头一个外链） | 供应商健康块头增加「配置体检」入口，与「账户详情」并列 | 新增只读体检页需要入口；两枚外链同放块头，避免再动页头的按钮组 | 待用户确认 |

未偏离项：五块顺序与优先级、短码集合、周期与维度的选项、状态文字的 aria-label 保留。
