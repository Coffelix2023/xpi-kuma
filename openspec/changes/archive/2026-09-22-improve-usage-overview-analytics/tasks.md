## 1. 原型先行

- [x] 1.1 使用 `xpi-prototype-design` 为 usage overview 创建 wireframe 原型，覆盖时间范围、消费指标、趋势、provider/model 排行、项目排行、效率排行和建议卡片，并验证产物位于版本化 prototype-design 目录
- [x] 1.2 在原型中补齐有数据、无数据、真实延迟不可用、样本不足和建议不可用状态，并通过桌面与小于800px视口预览确认核心语义可读
- [x] 1.3 根据原型评审结果固定生产面板的信息层级和建议卡片布局，记录未采纳的复杂图表或交互并确认不扩大本变更范围

## 2. 数据模型与采集

- [x] 2.1 核对当前 Pi 安装版本的事件类型，确认请求开始、首个响应 token、完成时间和结果状态是否可靠可得；用类型检查或最小事件测试记录结论，不能可靠获取时验证效率字段保持空值
- [x] 2.2 扩展 usage 领域类型，加入可选真实调用时间点、结果状态、缓存命中率和洞察所需的有界统计类型；运行 `pnpm typecheck` 验证类型兼容
- [x] 2.3 扩展 SQLite `usage_records` schema 与幂等迁移，保留旧数据并让旧记录的新字段为空；补充旧库迁移、重复迁移和 NULL 语义测试
- [x] 2.4 更新 usage collector 只记录可验证的真实调用时间点，不使用 probe、相邻记录或固定值估算；补充 assistant、缺失时间点和非 assistant 场景测试

## 3. 聚合与洞察

- [x] 3.1 扩展 provider/model 与项目归因聚合，返回费用占比、单请求成本、缓存命中率和有界排行数据；补充分母为零、未知项目和总和一致性测试
- [x] 3.2 增加真实调用效率聚合，按 provider/model 计算样本数、成功率、p50/p95 TTFT 和总响应时间，应用至少10条样本门槛；补充离群值、NULL、失败请求和样本不足测试
- [x] 3.3 实现成本、效率、缓存三类只读洞察，输出对象、依据、样本数、时间范围和置信度，并验证洞察模块失败不影响基础统计
- [x] 3.4 扩展 dashboard API 响应，统一返回 overview、stats、attribution、efficiency、cache 和 insights；验证既有字段保持兼容且响应数据有界

## 4. 生产面板

- [x] 4.1 按已评审原型更新使用量总览布局，增加缓存命中率、消费结构、provider/model 排行和项目费用排行；按设计决策 7 以 CSS 移植 shadcn base 视觉（既有 token，禁 React/构建/外部资源）；验证现有主题、语言、无外部资源和数字等宽字体约束仍满足
- [x] 4.2 增加效率排行和建议卡片，区分暂无真实数据、样本不足、不可用和真实结果，并展示依据、样本数、时间范围和置信度；验证建议不会触发配置写入或模型切换
- [x] 4.3 保持轮询、时间范围切换、空数据引导和响应式行为一致；用 dashboard UI/API 测试覆盖刷新不并发、窄视口排行可读和基础统计降级
- [x] 4.4 增加中英文文案与可访问名称，确保 p50/p95、缓存命中率和未知值不会只依赖颜色表达；运行相关 UI 文案测试

## 5. 集成验证

- [x] 5.1 使用受控 SQLite fixture 验证同一时间范围内总览、排行、项目归因、效率和洞察的费用/tokens/请求数口径一致
- [x] 5.2 执行 `pnpm typecheck`、`pnpm -w run lint` 和 `pnpm test`，修复所有失败后再继续
- [x] 5.3 通过本地 dashboard 预览检查桌面与窄视口真实渲染，确认原型评审结论已反映到生产布局，并记录验证结果
